{
  "name": "@airscan/monorepo",
  "version": "0.0.0",
  "workspaces": ["backends/*", "frontends/*", "packages/*"],
  "scripts": {
    "postinstall": "cd packages/engine && bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12"
  }
}